{
  "name": "senpai-x-waifu",
  "version": "1.0.0",
  "description": "Telegram bot using Telegraf and MongoDB",
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "telegraf": "^4.16.0",
    "mongodb": "^6.x",
    "dotenv": "^16.x"
  },
  "devDependencies": {
    "typescript": "^5.x",
    "tsx": "^4.x",
    "@types/node": "^22.x"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
